import React from 'react';
import classes from './ContactBackground.module.css';

const ContactBackground = props => {
    return (
        <div className={classes.Wrapper}></div>
    );
}

export default ContactBackground